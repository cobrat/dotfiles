#:schema https://yazi-rs.github.io/schemas/theme.json

[flavor]
dark  = "flexoki-dark"
light = "flexoki-dark"

# Flat statusline (no Powerline arrows).
[status]
sep_left  = { open = "", close = "" }
sep_right = { open = "", close = "" }

[indicator]
padding = { open = "", close = "" }

[which]
separator = " > "
